# Postgres connection for @cmc/db (Prisma). Not required for `prisma generate`
# or the P0 smoke test; needed once real data stories connect.
DATABASE_URL="postgresql://user:password@localhost:5432/cmc_edu?schema=public"
