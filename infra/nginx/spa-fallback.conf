# Single-page app fallback — serves index.html for any unknown path.
# Used inside the lms/admin containers (not the public-facing nginx).
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
}
