{
  "name": "@cmc/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CMC EDU MCP server — read-only tool surface for AI agents (CRM, finance, reconciliation).",
  "bin": {
    "cmc-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^6.0.0"
  }
}
