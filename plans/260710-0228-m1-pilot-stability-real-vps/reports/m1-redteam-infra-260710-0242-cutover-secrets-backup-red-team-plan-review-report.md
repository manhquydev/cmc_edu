# Red-team INFRA/OPS — M1 Pilot Stability / VPS cutover

Reviewer: m1-redteam-infra · Date: 2026-07-10 · Posture: adversarial (assume plan wrong until proven)
Scope: plan.md + phase-01/02/03/05/06, cross-checked vs `scripts/{backup-db,restore-drill,env-check,isolation-check}.sh`, `docs/runbook-deploy.md`, `docker-compose.prod.yml`, `.env.prod.example`, migrations, golive phase-02/04.

## Findings

| # | Sev | Claim in plan (file:line) | Evidence (actual) | Fix proposal |
|---|-----|---------------------------|-------------------|--------------|
| 1 | **Critical** | "Compose postgres init tự tạo `cmc_app`; nếu restore vào DB do compose khởi tạo thì role đã có" (`phase-03:26-28,72`); runbook "The compose postgres init creates it automatically; a bare new server does not" (`runbook-deploy.md:84-85`) | FALSE. `cmc_app` is created by the **migration** `20260706054322_p1_remediation_wave1_schema_rls/migration.sql:151-152`, not by compose. `docker-compose.prod.yml:124-139` postgres mounts only pg-data; **no init script**. `.env.prod.example:14` confirms: "Created by the p1_remediation_wave1_schema_rls migration." So on a fresh VPS, `cmc_app` does NOT exist until `prisma migrate deploy` runs. | Correct the claim everywhere. For P3 restore, `cmc_app` must be created explicitly (`CREATE ROLE cmc_app LOGIN NOSUPERUSER...`) before `pg_restore` of the ACL-inclusive dump, exactly as the DR caveat at `runbook-deploy.md:79-86` already says for "a bare new server." Reconcile the contradictory sentence. |
| 2 | **Critical** | P2 runs `prisma migrate deploy` + seed super_admin on VPS `cmc_prod` (`phase-02:71-77`); then P3 restores full ACL-inclusive custom-format dump **into the same `cmc_prod`** (`phase-03:73-77`). | Ordering collision. After P2, `cmc_prod` already contains all 49 tables + `cmc_app` + seeded super_admin/facility. A full `pg_restore --no-owner` (no `--clean`) into that DB errors "relation already exists" on every object and produces PK/data conflicts. `restore-drill.sh:96-100` handles this only for the *throwaway* (DROP+CREATE fresh); the **real** cutover restore is hand-waved ("dùng luồng restore riêng nhưng cùng flags", `phase-03:74-75`) and never drops/recreates `cmc_prod`. | Decide explicitly: (a) restore into a **fresh empty** `cmc_prod` — DROP DATABASE + CREATE + `CREATE ROLE cmc_app` before restore, so P2 must NOT migrate/seed the pilot DB (only bootstrap enough to verify boot on a separate throwaway), OR (b) `pg_restore --clean --if-exists`. Document the chosen path with exact commands; current plan silently assumes an empty target. |
| 3 | **Critical** | P2 rotates `BACKUP_ENCRYPTION_PASSPHRASE` to a **new** value on VPS (`phase-02:35,109`, `phase-02:82`); P3 cutover dump is produced on local-sim then decrypted on VPS (`phase-03:50-55,73`). | Passphrase mismatch blocks cutover. The P3 dump is `openssl enc`-encrypted on local-sim with the **local-sim** passphrase (`backup-db.sh:55-56`). P3 restore on VPS sources `.env.prod` whose `BACKUP_ENCRYPTION_PASSPHRASE` is now the **new** value → `openssl enc -d` (`restore-drill.sh:92-93` / manual restore) fails "bad decrypt." Plan never reconciles which passphrase decrypts the cutover dump. | Either produce the cutover dump on local-sim **using the new VPS passphrase** (export it before running `backup-db.sh`), or restore the cutover dump on VPS with the **old** passphrase then re-key forward. State it in P3 steps 2/4. Also: rotating the passphrase orphans every pre-cutover R2 backup unless the old passphrase is retained in escrow. |
| 4 | **High** | "Trên VPS DB reachable trong net trực tiếp" (`phase-03:34`); "Reuse scripts nguyên trạng" (`phase-05:48`); P3 step 6 runs `restore-drill.sh` on VPS (`phase-03:78`). | `backup-db.sh`/`restore-drill.sh` run on the **host** and connect to `DATABASE_URL`/`APP_DATABASE_URL` host = `postgres` (`.env.prod.example:11,15`), which only resolves **inside** the compose net (`runbook-deploy.md:49-56`). `restore-drill.sh:36` `DRILL_PG_URL` defaults to `localhost:5432` — postgres is **not** port-mapped (`docker-compose.prod.yml:124-134`). So host-run scripts fail to reach DB on VPS exactly as on local-sim (which needed the `cmcv2-pgfwd:15432` socat sidecar, `phase-02 golive:84`). P5:57 acknowledges this for cron but P3 restore-drill does not resolve it. | Define ONE concrete reach-DB mechanism for VPS host-run scripts (throwaway container attached to `cmcv2-prod-net`, or temporary internal port publish) and bake it into P3 (restore drill), P5 (cron) and the runbook. The claim "reachable trực tiếp" is only true from inside the net, not from host cron. |
| 5 | **High** | "rollback path = local-sim còn sống ... DNS revert được" (`phase-03:34,57,81,108`; stop-condition `plan.md:73`). | Rollback path is likely non-functional for external traffic. DNS A record points domain → **VPS** from P1 (`phase-01:65`, needed for LE HTTP-01). Local-sim runs on a **dev machine** (Windows, docker via Git Bash — ops memory) with **self-signed** TLS (`phase-02 golive:23`) and no established public routing for the real domain. Repointing DNS back to local-sim gives: (a) browser TLS errors (self-signed cert for the real domain), (b) requires local-sim to be publicly reachable on 443, never established in any phase. Data survives (not torn down) but the **serving** rollback is degraded/illusory. | Either establish local-sim as a genuine warm standby (public IP + valid cert for domain) — unlikely — or redefine rollback honestly: data rollback = restore the pre-cutover R2 dump onto VPS / fix-forward on VPS. Don't claim DNS-revert-to-local-sim as a clean rollback in a stop-condition without proving reachability. |
| 6 | **High** | "BREVO_API_KEY verify hoạt động trên VPS (rotate thuộc M0 P-1, M1 chỉ verify)" (`plan.md:42`). | Contradicted by M0's own addendum: `BREVO_API_KEY` in `.env.prod` returns **401 Key not found** live-verified 2026-07-10, "chưa từng có email Brevo thật gửi thành công" (`phase-04 golive:98-100`). No rotation has occurred. `env-check.sh:22` only asserts the var is **non-empty**, not that it works → boot passes with a dead key, and parent/student OTP email (a core pilot function) silently fails. | Track Brevo key rotation as an explicit M1 prerequisite/blocker (not "already done in M0"). Add a live send-verification step before P6 soak counts, since env-check cannot catch a 401. |
| 7 | **Medium** | Freeze window: "đưa local-sim về read-only (dừng writer / maintenance)" (`phase-03:44,69`). | No concrete read-only mechanism exists. App has no maintenance-mode flag; the worker is one writer but staff/parent API also writes. "dừng writer" ≠ read-only unless the whole `api` container is stopped (= full downtime). If api stays up, writes between dump and cutover are lost — the exact risk the freeze targets (`phase-03:106`). | Specify the enforcement: stop `api` + `worker` containers (accept downtime) or `ALTER DATABASE cmc_prod SET default_transaction_read_only = on` before dump. "Read-only" must be a real switch, not prose. |
| 8 | **Medium** | P6 exit "≥2 tuần không CRITICAL", "measured/objective" (`phase-06:24-45,79-85`); risk table "alert nếu thiếu backup mới / cert sắp hết hạn" (`phase-06:94`, `phase-05:91`). | No monitoring/alerting exists in repo — the "alert" is aspirational. CRITICAL rubric leaves "stack down > **X** phút" with X undefined (`phase-06:42`). Incident/postmortem template does not exist yet (must be created, `phase-06:59`). Detection of a CRITICAL (RLS breach, silent backup failure, cert expiry) is entirely manual. | Define X; create the template before soak starts; add a minimal objective signal (uptime check + "no backup newer than 26h" + cert-expiry check) so exit is measurable rather than "no one noticed an incident." |
| 9 | **Medium** | Escrow of `BACKUP_ENCRYPTION_PASSPHRASE` treated as inherited-from-M0, re-verify only (`phase-01:96`, `phase-05:41`). | M0 escrow was **never confirmed done** — "user CẦN escrow vào password manager, chưa xác nhận" (`phase-02 golive:81`), still pending in the 2026-07-10 addendum (`phase-04 golive:94`). Combined with the passphrase rotation (finding #3), the escrow chain has an open gap that P6 exit depends on (`phase-05:93`). | Treat escrow of the **new VPS** passphrase as a hard, unchecked user-action gate (P5 step 6 does verify recoverability — good), and don't assume M0 escrow happened. |

## KHÔNG phải issue (verified vững)

- **6 fix-forward bugs landed on main** (`phase-02:29-30`): verified `.gitattributes` has `*.sh text eol=lf`; `infra/nginx/nginx.conf:42` has `resolver 127.0.0.11 valid=10s;` and all `proxy_pass` use `set $..._upstream` variables (lines 79-143); LMS rewrite present (`:137-143`). Image built from main will carry these. OK.
- **RT-13 backup-host guard** (`backup-db.sh:24-32`, `restore-drill.sh:55-63`): comparing `hostname -f` vs the R2 endpoint host — for Cloudflare R2 these always differ, so RT-13 correctly passes and enforces off-box intent. Not a defect.
- **DB port not exposed** (`docker-compose.prod.yml:124-134`, no `ports:`): intentional, matches firewall plan (`phase-01:90`). Security-correct (it is the *cause* of finding #4, but the closed port itself is right).
- **`?schema=public` strip** (`backup-db.sh:45`) and **ACL-inclusive dump** (`--no-owner`, no `--no-acl`, `backup-db.sh:50`, `restore-drill.sh:106`): both fixes present and correct; GRANTs to `cmc_app` are preserved.
- **temp-file `trap ... EXIT`** present in both scripts (`backup-db.sh:40`, `restore-drill.sh:51`) — plaintext PII dump removed on abort. OK.
- **Drill guard against `cmc_prod`** (`restore-drill.sh:40-44`) and **`BACKUP_BUCKET_PRIVATE_CONFIRMED` gate** (`restore-drill.sh:28-33`, `env-check.sh:51-56`): present, fail-closed. OK.
- **isolation-check trivially passing on clean VPS** (`phase-01:54`): acknowledged, and the non-docker port squatter gap is covered by the manual `netstat` probe in P2 step 3 (`phase-02:68`, pattern from `phase-02 golive:33`). OK.
- **Session secret rotation invalidates staff/LMS cookies**: rotating `STAFF_SESSION_SECRET`/`LMS_SESSION_SECRET` on VPS (`phase-02:34-35`) only invalidates existing signed cookies (forces re-login) — no data impact. Correct to rotate; not a hidden loss.

## Unresolved questions

1. Does P2 intend to `migrate deploy` + seed the **pilot** `cmc_prod`, or a separate bootstrap DB? This decides whether finding #2's collision is real — plan reads as same DB.
2. Is local-sim ever publicly routable on the real domain with a valid cert (finding #5)? If not, the DNS-revert rollback stop-condition is unenforceable as written.
3. Which passphrase (old local-sim vs new VPS) is authoritative for the cutover dump, and is the old passphrase retained in escrow for pre-cutover backups (finding #3)?
4. What is the concrete reach-DB path for host-run backup/restore on VPS (throwaway container vs temp port) — needed by both P3 and P5 (finding #4)?
5. Was `BREVO_API_KEY` rotated to a working key, or is a dead key going into the pilot (finding #6)?
