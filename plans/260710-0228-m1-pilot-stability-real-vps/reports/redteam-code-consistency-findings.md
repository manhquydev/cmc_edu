# Red-team review — M1 Phase 4 (code hardening) + cross-phase consistency

Reviewer: m1-redteam-code · Date: 2026-07-10 · Posture: adversarial (assume plan wrong until proven)
Scope: `phase-04-hardening-code.md` verified against live code; consistency across `plan.md` + phases 1–6.
Prisma 6.19.3 (`pnpm-lock.yaml`), PostgreSQL, `EmailOutbox.payload` is `Json` → `jsonb`.

## Findings

| # | Sev | Plan claim (file:line) | Evidence in real code | Fix / recommendation |
|---|-----|------------------------|-----------------------|----------------------|
| 1 | HIGH | (a) sweep fix = add `NOT: { payload: { path: ['scrubbed'], equals: true } }` — `phase-04:31,71-73` | Sweep must MATCH unscrubbed rows `{kind:'otp',code:...}` which have NO `scrubbed` key (`relay-email-outbox.ts:63,74-80`). Top-level `NOT` on a JSON path-equals compiles to `NOT (payload#>'{scrubbed}' = 'true')`; for a missing key `payload#>'{scrubbed}'` is SQL NULL → `NOT (NULL = true)` = NULL → row **excluded** (3-valued logic). Result: the clause drops *unscrubbed* rows (NULL) AND scrubbed rows (NOT TRUE=FALSE) → sweep scrubs **zero rows**, breaking C1 (OTP plaintext past TTL never age-scrubbed). Prisma docs do not add an `IS NULL` guard for the generic `NOT` wrapper (confirmed no documented guard). | Do NOT use path-based `NOT`. Use whole-object jsonb inequality (both operands non-null → no NULL trap): `NOT: { payload: { equals: SCRUBBED_OTP_PAYLOAD } }` alongside the existing `payload:{path:['kind'],equals:'otp'}`. This matches unscrubbed (payload≠sentinel→TRUE) and excludes scrubbed (equals→NOT→FALSE). MUST add a positive test asserting an unscrubbed stale row still gets scrubbed — note the existing test `sweeps a stale OTP row past its TTL` (`relay-email-outbox.test.ts:181-190`) already fails under the broken clause, so gates catch it, but the plan's prescribed syntax is wrong as written. |
| 2 | HIGH | (b) `pruneTerminalOutbox` called inside `relayEmailOutbox` after sweep via `deleteMany` — `phase-04:34,55-57,76` | The 9 unit tests build `makeMockDb()` which stubs only `emailOutbox.{findMany,updateMany,update}` + `auditLog.create` (`relay-email-outbox.test.ts:244-255`). No `emailOutbox.deleteMany` stub. A `deleteMany` call inside the function under test → `TypeError: not a function` → **all RT-6/RT-8 unit tests crash**, failing the "unit 525" gate. Plan does not mention updating the mock. | Add `deleteMany: vi.fn().mockResolvedValue({count:0})` to `makeMockDb()`. If a `pruned` field is added to `RelayEmailOutboxResult` (natural, matching `otpSwept`/`reaped`), EVERY `expect(result).toEqual({...})` (unit lines 286,319,343,368,387 + integration) breaks and must be updated. Plan should call this out or keep prune as a separate worker-cycle function NOT inside `relayEmailOutbox`. |
| 3 | MEDIUM | (b) `@@index([status, createdAt])` "covers the 3 seq-scan queries incl. cap-count" — `phase-04:32-37` | Cap-count query filters `transport='brevo' AND payload.path['kind']=='otp' AND createdAt>=hourAgo` — **no `status` column** (`lms-auth/router.ts:316-318`). A `[status, createdAt]` index has `status` as leading key → unusable for a predicate that never constrains status. The OTP flood-guard (runs on every OTP request, the DoS-amplification hot path) stays seq-scan. Reap (`status='sending' AND updatedAt<cutoff`, `:125-128`) uses only the status prefix (index has createdAt, not updatedAt) — partial. | Index choice is fine for drain; but if cap-count perf matters, add `@@index([transport, createdAt])` (or a partial index on kind). Plan already defers GIN as YAGNI — acceptable IF it explicitly states cap-count remains unindexed and is bounded only by retention. Note retention (sent/dead, 30d) does NOT prune `failed`/`pending` OTP rows, so an attacker's burst within the hour still seq-scans. |
| 4 | MEDIUM | "P4 độc lập, chạy song song P1–P3" (`plan.md:37-38`) vs P4 ships a DB migration | P3 restores a **local-sim `cmc_prod` dump** into VPS (`phase-03:40,73-75`, `pg_restore --no-owner`, no `--data-only`/`--section` specified). Local-sim runs the OLD image and never gets P4's migration, so its dump's `_prisma_migrations` table lacks the P4 row and its schema lacks the index. P2 already ran `migrate deploy` on VPS (`phase-02:71`) creating the index. Restoring the local-sim dump on top can (a) reintroduce a `_prisma_migrations` state without the P4 row → next `migrate deploy` re-applies → `relation already exists` error, or (b) with `--clean` drop the P2 index. Plan's mitigation (`phase-04:104,114-115`) only ensures the *image* has the migration ("land P4 before P2 build"); it does NOT address dump/restore `_prisma_migrations` drift. This is the planner's real open risk. | Specify pg_restore mode in P3 (recommend `--data-only` into the P2-migrated schema, or exclude/reconcile `_prisma_migrations`). OR apply P4's migration to local-sim before the P3 dump so dump schema == VPS schema. Add an explicit `migrate deploy` (idempotent no-op) + `migrate status` check as a post-restore step. The "fully independent / parallel" framing in plan.md:37-38 is too strong given P4 mutates DB schema that P2/P3 touch. |
| 5 | LOW | Gate stated as "unit tổng 525/525" (`phase-04:48,49,95`) | P4 also *adds* tests: sweep no-reamplify (`:73`) + prune seed/keep (`:77`). Fixing receipt-get is 524→525, but new tests push the total >525. The "525/525" number is stale the moment new tests land. | State the gate as "all unit green (≥525)" rather than a frozen count. |
| 6 | LOW | (c) counter upsert "không cần đổi" premise (`phase-04:80-81`) | Correct and consistent: `ReceiptCodeCounter` cleanup uses plain `db.*` not bypass (`test/db.ts:180` + comment `:171-172` "sentinel RLS policy"), so its upsert works without a facility GUC. Only `db.receipt.create` (`receipt-get.test.ts:72`) hits 42501. | No change — premise verified. Informational. |

## NOT an issue (verified, do not "fix")

- **(c) receipt-get fix via `testDbBypass` is correct and idiomatic.** `test/db.ts:75-83` explicitly lists `Receipt` as a `testDbBypass` arrange target; every seed helper (`seedActiveEnrollment`, `seedClassBatch`, `seedClassSession`) uses the same bypass for RLS tables. The ACT under test (`director.finance.receiptGet` via router → `withFacility`) still exercises RLS, so bypassing only in arrange does NOT weaken the test. `withFacility(testDb(), facility.id, …)` would be marginally more realistic but bypass matches repo convention. The pre-existing failure premise (naked `cmc_app` INSERT → 42501 under FORCE RLS with no facility GUC) is fully consistent with the entire `test/db.ts` design — credible; confirm by running the single file once cmc-pg is up.
- **Sweep-only change does NOT break the mock sequence.** The unit mocks drive `updateMany` via ordered `mockResolvedValueOnce` (reap→claim→sweep, e.g. `:272-275`). Adding a WHERE clause keeps sweep as one `updateMany` call → sequence intact. Only the *prune* (`deleteMany`) breaks the mocks (finding #2).
- **Write-amplification (finding-a premise) is real.** Post-scrub payload `{kind:'otp',scrubbed:true}` still satisfies `path['kind']=='otp' AND createdAt<cutoff` (`:63,74-80`) → every cycle re-UPDATEs all historical scrubbed OTP rows unconditionally. HIGH severity confirmed.
- **`brainstorm-260710-0215-...` context link resolves** to `plans/reports/brainstorm-260710-0215-project-status-verification-next-plans-pipeline-report.md` (repo-level `plans/reports/`, not the plan-local empty dir). Not dangling.
- **Task graph is acyclic and matches plan.md.** TaskList: #2←#1, #3←#2, #5←#2,#3, #6←#2,#3,#4,#5, #4 independent — matches `plan.md:39-40` and phase `dependencies:` frontmatter. `plan.md` = 76 lines (<80), frontmatter well-formed, `blockedBy: [260707-2308-golive-sprint-land-sso-env-uat]` valid.
- **Retention prune does not touch retryable OTP rows.** Prune targets `status IN (sent,dead)` only; `failed`/`pending` (still holding OTP for resend) are preserved (`phase-04:77,103`). No conflict with ADR validation #2. Sweep still scrubs stale `failed` payloads past TTL as backstop.
- **Invariants consistent across phases.** STAFF≠LMS secret, 5-role cap, cmc_prod-never-in-tests, encrypted backups — no contradiction between plan.md and phase files.

## Unresolved questions

1. **Finding #1 direction — can it be confirmed against live PG?** Static SQL/3-valued-logic reasoning is high-confidence, but I did not execute against cmc-pg. A 2-minute check (`updateMany` with the proposed NOT clause on a seeded unscrubbed row, assert count) would make it empirical. Recommend the implementer verify before committing the exact clause.
2. **Finding #4 — what pg_restore mode does P3 actually use?** Plan says `--no-owner` but omits `--clean`/`--data-only`/`--section`. The `_prisma_migrations` drift risk hinges entirely on this. Needs the planner to pin the restore mode.
3. **Is receipt-get.test.ts currently red?** High-confidence yes from RLS design, but unconfirmed at runtime (cmc-pg not started in this review).
