# Red-team: Odoo UX Grammar Full Adoption

**Date:** 2026-08-03  
**Mode:** adversarial review of `plans/260803-2043-odoo-ux-grammar-full-adoption`  
**Verdict:** **PASS WITH FIXES** — cook allowed after integrating the mandatory patches below into plan/phases.

---

## Hostile findings

### RT-1 CRITICAL — “Full adoption” metric is fuzzy

**Attack:** Phase 8 says “zero unintentional off-frame” but never defines the file inventory or grep recipe. Cook will argue forever.

**Fix (mandatory):** Phase 8 must include an explicit exclusion list:

```text
EXEMPT: login.tsx, change-password.tsx, design-lab.tsx, pdf-annotator.tsx,
        *dialog*.tsx, use-*.ts, *-actions.ts, coming-soon.tsx
```

Adoption = every other `apps/admin/src/pages/**/*.tsx` default export page imports one of ListPage|DetailPage|FormPage|DashboardPage **or** is listed under EXEMPT with reason in VIEW-GRAMMAR.

### RT-2 HIGH — Parallel phases 4–7 race on shared chrome

**Attack:** Subagents touch FilterBar/PageHeader while phase 2 lands ControlBar → merge hell.

**Fix:** Phase 2 is hard gate. Phases 4–7 may only edit **page files under their ownership table**; no edits to `packages/ui/**` after phase 2 without a stop-the-line.

Ownership:

| Phase | Owns paths only |
|-------|-----------------|
| 4 | students/, classes/, courses/, enrollment/ |
| 5 | crm/pipeline*, finance/revenue*, finance/refund* |
| 6 | hr/*, admin/shift-config* |
| 7 | parents/*, engagement/leaderboard*, teaching/grading* |

### RT-3 HIGH — Pipeline ListPage may destroy UX

**Attack:** Forcing ListPage table assumptions breaks kanban columns (scroll, height).

**Fix:** Phase 5 success criteria: pipeline must keep FunnelBar + board; ListPage `isEmpty` false when board has columns; no forced DataTable shell. Add manual/visual check note (screenshot optional).

### RT-4 MEDIUM — payroll dual-mode hooks

**Attack:** Conditional ListPage vs DetailPage after hooks → Rules of Hooks violation.

**Fix:** Phase 6: always call same hooks; branch only in JSX return frames.

### RT-5 MEDIUM — ListPagination API mismatch

**Attack:** receipt-list pager state shape ≠ ListPagination props → silent wrong pages.

**Fix:** Phase 8 step 0: read ListPagination props + receipt-list pager state; map 1:1 in phase notes before code.

### RT-6 LOW — Phase 1 title file still `phase-01-start.md`

**Attack:** Cook confuses “Start” with empty scaffold.

**Fix:** Title in frontmatter already “VIEW-GRAMMAR law”; plan table names it correctly — OK if body is filled (it is).

### RT-7 HIGH — Scope B3 vs solo capacity

**Attack:** 8 phases across 15 pages → abandoned mid-plan, worse than B1.

**Fix:** Quality gates already chosen. Add rule: if a phase fails twice, cut to min frame + EmptyState for that page and file DONE_WITH_CONCERNS — do not block entire plan.

### RT-8 MEDIUM — Missing rollback

**Attack:** Bad ListPage wrap ships broken CRM.

**Fix:** Each phase: git-revert friendly; no shared migration. Prefer one commit per phase cluster.

### RT-9 LOW — Non-goal bulk may be re-requested mid-cook

**Attack:** User says “also bulk” mid-phase 4.

**Fix:** Plan non-goals are binding; new work = new plan. Red-team records this.

### RT-10 MEDIUM — No typecheck gate written

**Fix:** Each phase success: `pnpm --filter @cmc/ui test` and/or admin vitest for touched files; phase 8: admin typecheck if public exports changed.

---

## What the plan gets right

- Named ControlBar (discoverable)
- Quality gates before coverage race
- Pipeline special-case (no KanbanBoard)
- Explicit non-goals (no OWL)
- Evidence chain from xia/scout/advise

## Required plan patches before cook

1. Add EXEMPT list + adoption definition (RT-1) → phase 8 + plan Success Criteria.  
2. Add file ownership table for parallel phases (RT-2) → plan.md Cook notes.  
3. Pipeline non-table body rule (RT-3) → phase 5.  
4. Hooks rule for payroll (RT-4) → phase 6.  
5. Failure cut rule (RT-7) → plan.md.  
6. Typecheck/test commands (RT-10) → plan.md Success Criteria.

## Residual risk after patches

**Medium** — volume of page migrations; mitigated by phase gates and cut rule.

**Cook recommendation:** Proceed after patches applied in-session (below).
