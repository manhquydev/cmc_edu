# Red-team — Calendar ClassSession timed gap

**Date:** 2026-08-04  
**Plan:** `plans/260804-calendar-session-timed-gap`  
**Stance:** hostile product + security + perf

## Attacks on the plan

| # | Attack | Verdict | Disposition |
|---|--------|---------|-------------|
| 1 | Unbounded date range → full table scan / memory | Valid | Cap 120 days; index on facilityId+sessionDate |
| 2 | Course filter bypass leaks other courses | N/A if filter optional only narrows; still facility RLS | Keep facilityId hard filter |
| 3 | Cancelled sessions still shown → wrong attendance path | Valid | Default exclude cancelled |
| 4 | Deep-link only sessionId fails attendance | Already known | Always include classBatch+session |
| 5 | Mixing all-day batch + timed doubles visual noise | Valid | v1 timed-only on calendar views |
| 6 | datesSet loop DoS client | Valid | String-compare before setState |
| 7 | Permission: sale sees teacher schedule | Acceptable — class.read already shared; same as list | No new permission |
| 8 | Drag reschedule implied by timeGrid | Out of scope | editable=false stays |
| 9 | Empty data looks like "broken calendar" | Product risk | Honest callout |
| 10 | N+1 alternative regresses perf | Rejected by advise | listInRange only |

## Must-fix before cook

- Range cap + from≤to validation
- Cancelled exclusion default
- Dual query params on href

## Accept residual

- Empty calendar when no sessions generated for window
- No drag/reschedule
- List/kanban still batch-centric

## Result

**PASS with mitigations baked into phase 1–3.** Safe to validate and implement.
