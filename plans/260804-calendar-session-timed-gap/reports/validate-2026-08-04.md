# Validate — Calendar ClassSession timed gap

**Date:** 2026-08-04  
**Plan:** `plans/260804-calendar-session-timed-gap`

## Critical questions (answered from scout + code)

| Q | Answer | Evidence |
|---|--------|----------|
| What is the source of truth for lesson time? | `ClassSession.startTime`/`endTime` timestamptz | schema.prisma ClassSession |
| Can UI get range without N+1? | Not today — only list by batch | class-session-router `list` |
| Attendance deep-link needs what? | classBatch + session | attendance.tsx searchParams |
| FC already supports timed events? | Yes; shell has timeGrid + eventsFn | soft-ops-fullcalendar.tsx |
| Migration needed? | No | Index exists |
| Permission model? | class.read | same as list |
| Max range? | 120 days | red-team mitigation |
| Keep batch all-day on calendar? | No in v1 | clarity |

## Consistency sweep

- Phases 1→4 sequential; no renamed APIs across files.
- Advise Option B matches all phases.
- Non-goals aligned (no drag, no premium, no Soft Ops re-skin of FC).
- Zero unresolved contradictions.

## Acceptance (implementation gate)

1. listInRange works with facility scope + caps  
2. Timed FC events + dual deep-link  
3. schedule week/month use listInRange  
4. Tests green  

## Result

**VALIDATED.** Proceed to implement phases 1–4.
