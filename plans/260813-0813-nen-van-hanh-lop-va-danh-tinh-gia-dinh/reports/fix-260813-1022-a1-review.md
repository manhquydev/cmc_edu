# Fix A1 review — F3 / F1 / F2 / F5

**Ngày:** 2026-08-13 · **Nhánh:** `feat/lms-class-schedule-foundation`  
**Base:** `8d83931` (A1). Commit mới, không amend.

## Đã làm theo quyết định

### F3 — `teacherId` NULL = kế thừa GV lớp
- Bỏ `UPDATE ClassSession SET teacherId = cb.teacherId` khỏi `20260813093900`.
- Migration mới `20260813102200_session_teacher_null_means_inherit`: gỡ bản sao đã đóng băng (`teacherId` buổi == `teacherId` lớp → NULL). Override thật (khác lớp) giữ nguyên.
- Sinh buổi mới **không** ghi GV lớp lên cột (để `assignTeacher` của lớp tự hiện).
- Mọi DTO đọc: `row.teacherId ?? row.classBatch.teacherId` (`list`, `get`, `listInRange`, cancel/confirm/assignUnit/assignTeacher).
- `classBatch.assignTeacher` không lan xuống buổi.
- Test: lớp lập không GV → gán GV lớp → mọi buổi hiện GV đó; cột DB vẫn NULL. Gán GV riêng một buổi: chỉ buổi đó khác.

### F1 — `schedule.addSlot`
- Thủ tục mới, `requirePermission('schedule', 'generate')`, audit middleware.
- Test: archive hết khung → generate = 0 → addSlot → generate tạo buổi mới.

### F2 — regenerate chỉ từ hôm nay ICT
- `generateSessions` lập kế hoạch từ `max(startDate, hôm nay ICT)`.
- `classBatch.create` / `lmsOps.createClassWithUnits` vẫn lập từ `startDate`.
- Test mới: đổi thứ rồi sinh lại → mọi buổi thứ mới đều ≥ hôm nay.

### F5
- `classSession.assignTeacher` gọi `assertSessionActive(session, { alsoBlockDone: true })`.
- Test: cancelled / done → `BAD_REQUEST`.

### Thấp / không sửa
- `insertMissingPlannedSessions`: `created = createMany().count`; `where` có `facilityId`.
- Giữ `select classBatch.teacherId` ở get/listInRange (cần fallback F3).
- F4: không đổi KPI/quyền. Comment trên cột schema + thủ tục `assignTeacher`: nhãn hiển thị, không phải nguồn quyền/chấm công.
- F6: không `REVOKE DELETE`. Comment trong migration 20260813093900.

## F2 — test cũ dựa vào buổi quá khứ (không nới)

Hôm nay ICT = **2026-08-13**. Hai test trong `generate-sessions.test.ts` **không sửa**:

| Test | Kỳ vọng cũ | Vì sao đỏ sau F2 |
|---|---|---|
| `adds only the new sessions when the range is extended` `:130-150` | Nới `endDate` 2026-08-03 → 08-17 tạo **2** Monday (10 và 17) | 10/08 đã qua. Regenerator cắt từ 13/08 → chỉ tạo 17/08. `sessionsCreated` = **1** |
| `enforces the room conflict on extend` `:226-249` | Lớp B nới tới 10/08 trùng phòng lớp A | 10/08 đã qua. Planned rỗng từ hôm nay → **không CONFLICT**, `sessionsCreated` = 0 |

Đúng hành vi F2. Muốn test còn sống thì phải đổi fixture sang ngày ≥ hôm nay (hoặc freeze đồng hồ) — **chưa làm**, đúng lệnh.

## Validation (kết quả thật)

`DATABASE_URL` / `APP_DATABASE_URL`: **present** (source `packages/db/prisma/.env`, không in giá trị).

```
pnpm typecheck --force
→ Tasks: 34 successful, 34 total; Cached: 0/34; Time: 49.688s; exit 0

pnpm test   # turbo run test --filter=!@cmc/e2e
→ Tasks: 29 successful, 30 total
→ Failed: @cmc/api#test
→ apps/api: Test Files 1 failed | 129 passed (130)
            Tests 2 failed | 1217 passed (1219)
→ đúng 2 case F2 ở trên. Không fail thêm.
→ Time: 2m47.862s; exit 1
```

Status: DONE_WITH_CONCERNS  
Summary: F3 inherit, addSlot, regenerate từ hôm nay, và chặn assignTeacher buổi done/cancelled đã vào. Typecheck lạnh xanh. `pnpm test` đỏ đúng 2 case cũ dựa vào sinh buổi quá khứ — không nới assertion.

---

## Tiếp — fixture ngày tương đối (sau khi không nới assertion)

Không có khuôn `vi.setSystemTime` cho class/schedule. Đổi sang thứ Hai ICT trên/sau hôm nay (`upcomingMonday(n)`).

Assertion **giữ nguyên độ chặt**: nới range → đúng 2 buổi mới / tổng 3; nới đụng phòng → `CONFLICT`.

| Test | Việc |
|---|---|
| nới `endDate` | `monday0` + `monday2` (2 tuần) |
| xung đột phòng khi nới | lớp A giữ `monday1`, lớp B tạo `monday0` rồi nới tới `monday1` |
| regenerate idempotent | cũng gọi `generateSessions` — chuyển sang `monday0` kẻo cửa sổ rơi vào quá khứ thành xanh giả |
| archive + khung cùng giờ | cùng lý do — `monday0` |
| F2 không sinh buổi trước hôm nay | `start = today-28`, `end = today+56` (tránh chết sau 2026-12-31) |
| `lms-ops` regenerate không nhân bản | cùng chạm `generateSessions` — 4 Monday từ `upcomingMonday(0..3)` |

Giữ nguyên `2026-08-xx` ở case **chỉ** `classBatch.create` (đếm buổi, conflict lúc tạo, ICT 08:00→01:00 UTC, mã lớp năm 2026, P2002, quyền, RLS). `assign-session-teacher` vẫn 2026-08-03..10 vì không gọi regenerate.

Không có test nào phải bỏ vì F2.

### Validation sau đổi fixture

```
pnpm typecheck --force
→ Tasks: 34 successful, 34 total; Time: 50.587s; exit 0

pnpm test
→ Tasks: 30 successful, 30 total
→ apps/api: Test Files 130 passed; Tests 1219 passed
→ Time: 2m55.469s; exit 0
```

Status: DONE  
Summary: Hai test đỏ đã chuyển sang thứ Hai tương đối so với hôm nay ICT, assertion cũ giữ nguyên. Typecheck 34/34 và `pnpm test` 1219/1219 xanh.
