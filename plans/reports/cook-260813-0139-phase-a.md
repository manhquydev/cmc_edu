# Cook report — Phase A precedence pin (2026-08-13)

**Worktree:** `/home/manhquy/.herdr/worktrees/cmc_edu/feat-token-name-isolation`  
**Branch:** `feat/token-name-isolation`  
**Commit:** `0e3597b` rồi `2e8c5cf` (vòng sửa review)  
**Pushed:** no

## Đã làm gì

1. **Sửa assertion nói dối** trong `packages/ui/src/astryx-theme-cmc.test.ts`. Test giờ nói đúng: trong shell admin (`.o_web_client` nằm trong `[data-astryx-theme]`), `console.css` cố ý thắng vì nó gán property trên chính phần tử; ngoài shell (LMS) mapping CMC/Astryx mới resolve. Kỳ vọng “file vẫn khai đủ `--font-size-*`” giữ nguyên. Rà `console-astryx-remap.test.ts` và `console-tokens.test.ts:34-41`: không sửa — comment không nói sai lý do, kỳ vọng vẫn đúng.
2. **Test precedence** `packages/ui/src/console/console-precedence.test.ts`: nạp `tokens.css` → `astryx-theme-cmc.css` → `console.css` (đúng thứ tự `apps/admin/src/main.tsx`) thành 3 thẻ `<style>`. Fixture: `.o_web_client` trong `[data-astryx-theme=neutral]`, node đối chứng cũng có attribute theme nhưng **ngoài** `.o_web_client`. Assert `--font-size-*` / `--color-text-*` / `--font-family-*` (vd. `--font-size-lg` 15px trong shell, 16px ngoài). Fail message in cả giá trị thắng lẫn giá trị bề mặt kia. Ghi rõ jsdom bỏ `@import` node_modules nên `--text-*` upstream không resolve ở đây.
3. **Cổng mapping upstream** cùng file, `it.skipIf(!existsSync(...))` đọc `node_modules/@astryxdesign/theme-neutral/dist/theme.css`. Assert mapping, không assert px: `--text-label-size` → `--font-size-base`, `--text-heading-3-weight` → `--font-weight-bold`, `--text-supporting-size` → `--font-size-sm`. Chạy thật (không skip) trên worktree đã `pnpm install`.
4. **Token ma** `apps/admin/src/pages/crm/report.tsx:136`: `var(--cmc-text-supporting)` → `var(--cmc-text-muted)`. Token gần nghĩa nhất đã khai trong `tokens.css` (Astryx supporting/secondary map sang `--cmc-text-muted`). Không đẻ token mới. Không đụng `shifts-detail.tsx` / `WS_CSS`.

GitNexus: `impact(SimpleTable)` LOW (chỉ `CrmReportPage`). `detect_changes` trước commit: 1 symbol, 0 process, risk LOW.

## Bằng chứng test đỏ rồi xanh

Tạm đổi `packages/ui/src/console.css` `--font-size-lg: 15px` → `99px`, chạy `vitest run src/console/console-precedence.test.ts`, rồi hoàn tác. `git diff -- '*.css'` sau hoàn tác rỗng; dòng 379 lại là `15px`.

**Lần đỏ** (sau mutation, 1 failed / 3 passed):

```
FAIL  src/console/console-precedence.test.ts > console / Astryx / CMC precedence > lets console.css win --font-size-* / --color-text-* / --font-family-* inside the admin shell
AssertionError: --font-size-lg resolved to "99px"; expected winner "15px"; other surface is "16px": expected '99px' to be '15px'
```

**Lần xanh** sau hoàn tác, rồi cả suite:

```
pnpm --filter @cmc/ui test
 Test Files  42 passed (42)
      Tests  152 passed (152)
```

Baseline trước phase: 41 file / 148 test. Delta: +1 file (`console-precedence.test.ts`) / +4 test. Upstream mapping không skip.

**Typecheck:** `pnpm typecheck` xanh — `Tasks: 34 successful, 34 total` (sau `prisma generate` trong worktree; lần chạy đầu đỏ vì cache Prisma `{}` của worktree khác, không liên quan Phase A).

**Review:** `ak-engineer:tester` DONE_WITH_CONCERNS (typecheck lần đầu đỏ, sau đó đã xanh). `ak-engineer:code-reviewer` 9/10, 0 critical, ship as-is.

## `git diff --stat` (commit `0e3597b`)

```
 apps/admin/src/pages/crm/report.tsx                |   2 +-
 packages/ui/src/astryx-theme-cmc.test.ts           |  13 +-
 packages/ui/src/console/console-precedence.test.ts | 210 +++++++++++++++++++++
 3 files changed, 219 insertions(+), 6 deletions(-)
```

Không có hunk CSS. Không có CI mới. Không đụng `console.css` 371–441 (trừ mutation tạm, đã hoàn tác), `shifts-detail.tsx`, `WS_CSS`.

## Lệch khỏi đặc tả — và lý do

| Lệch | Lý do |
|---|---|
| Không sửa `console-astryx-remap.test.ts` / `console-tokens.test.ts` | Đặc tả: giữ intent, chỉ sửa chỗ mô tả sai lý do. Hai file đó không nói “Astryx thắng trong shell”. |
| Assert `var(--console-gray-900,#212529)` và font-family `"Inter",-apple-system,...` (jsdom serialize, mất space / đổi quote) | `getComputedStyle` trả form đó, không phải literal trong `console.css`. Vẫn pin cùng declaration. |
| Node đối chứng là sibling `[data-astryx-theme=neutral]`, không nằm trong cùng wrapper với `.o_web_client` | Đặc tả yêu cầu đối chứng **ngoài** `.o_web_client`; LMS cũng có theme attribute. `:root` và `[data-astryx-theme]` cùng khai CMC nên kết quả giống nhau. |
| Không fail-closed `skipIf` khi `CI=true` (gợi ý reviewer) | Đặc tả bắt `skipIf(!existsSync)`; thêm hành vi CI là ngoài scope và cấm script CI mới. |
| Không sửa comment nói dối còn lại ở `astryx-theme-cmc.css:63-69` | Ràng buộc cứng: diff chỉ test + 1 dòng `report.tsx`. Để Phase B. |
| `pnpm typecheck` lần 1 đỏ; xanh sau `prisma generate` | Turbo cache worktree dùng Prisma rỗng. Không đổi source. `--force` không rebuild `@cmc/db` vì task `build` không khai `DATABASE_URL`. |
| Không cập nhật `plan.md` / phase file trong git worktree | Ràng buộc cứng về `git diff`. Báo cáo này nằm ngoài worktree (`Downloads/...`). |
| Commit trực tiếp, không spawn `git-manager` | User chốt conventional commit, không nhắc AI, không push. |

## Vòng sửa review (sau `review-260813-0139-phase-a.md`)

**Commit:** `2e8c5cf` `test(ui): pin all font-size steps and console gray colors` (không push)

Đóng F1 + F2 (chặn merge) và F3/F4/F6/F7 theo đúng việc bắt buộc.

### Sửa

1. **F1** — pin literal cả 12 bậc `--font-size-*` trên **con** của `.o_web_client` và trên con ngoài shell. jsdom không resolve `var()` khi gán `font-size`/`color` used-property; pin bằng `getPropertyValue` (giá trị specified, kế thừa xuống con) và một hop `var(--cmc-*)` cho `4xs`/`3xs`/`sm` phía CMC (`11px`/`11px`/`13px`). Xóa `--font-size-4xl: 22px` giờ đỏ: shell inherit `32px`.
2. **F2** — không còn so khớp chuỗi `var(--console-gray-900,#212529)`. Pin `--console-gray-900`/`--console-gray-600` hex + rgb used-color (`#212529` → `rgb(33, 37, 41)`). `--color-text-*` trong shell phải reference đúng gray token. Đổi `#212529` → `#ff0000` giờ đỏ.
3. **F3 + F7** — comment đúng cơ chế: khai trên `.o_web_client` thắng kế thừa; không phải vì lồng DOM hay thứ tự import. Sửa header + comment nạp sheet trong `console-precedence.test.ts`, câu cuối `astryx-theme-cmc.test.ts`, và `astryx-theme-cmc.css:63-69` (chỉ comment, không đổi giá trị).
4. **F4** — fixture thêm `span` con; mọi assert chạy trên descendant.
5. **F6** — `if (process.env.CI) expect(existsSync(UPSTREAM_THEME)).toBe(true)` trong test riêng (không bị `skipIf` nuốt).

### Chứng minh đỏ — `--font-size-4xl`

Comment-out `--font-size-4xl: 22px` trong `console.css`, chạy test, hoàn tác.

```
FAIL  ... lets console.css win ... on descendants of the admin shell
AssertionError: --font-size-4xl specified as "32px" → "32px"; expected "22px"; other surface is "32px"
FAIL  ... resolves different winners ...
AssertionError: --font-size-4xl shell="32px" outside="32px"
```

### Chứng minh đỏ — `--console-gray-900`

`--console-gray-900: #212529` → `#ff0000`, chạy test, hoàn tác.

```
FAIL  ... lets console.css win ... on descendants of the admin shell
AssertionError: --console-gray-900 specified as "#ff0000"; expected hex "#212529"
```

### Xanh sau hoàn tác

```
pnpm --filter @cmc/ui test
 Test Files  42 passed (42)
      Tests  153 passed (153)
```

(+1 test so với 152: cổng `existsSync` khi `CI`). `pnpm typecheck` 34/34 xanh. `console.css` không còn trong `git diff`.

### `git show --stat 2e8c5cf`

```
 packages/ui/src/astryx-theme-cmc.css               |  14 +-
 packages/ui/src/astryx-theme-cmc.test.ts           |   8 +-
 packages/ui/src/console/console-precedence.test.ts | 249 +++++++++++++--------
 3 files changed, 161 insertions(+), 110 deletions(-)
```

### Lệch vòng này

| Lệch | Lý do |
|---|---|
| Diff có `astryx-theme-cmc.css` (comment, không đổi giá trị) | Việc (3) và F7 bắt sửa comment nguồn. Ràng buộc “chỉ test + report.tsx” nhường cho lệnh sửa comment tường minh. |
| Không pin used `font-size: var(--font-size-*)` thành px | jsdom trả lại đúng chuỗi `var(...)`. Pin specified + một hop token là cách duy nhất chốt `4xl` 22 vs 32 trong jsdom. |
| Không amend `0e3597b` | Commit tiếp theo để giữ lịch sử vòng review. |

GitNexus `detect_changes` vòng này: 0 symbol, 0 process, risk LOW.

PHASE A FIXED
