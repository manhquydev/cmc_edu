# Cook — Phase B (docs + gates)

**Worktree:** `/home/manhquy/.herdr/worktrees/cmc_edu/docs-console-authority`  
**Branch:** `docs/console-authority` @ `cc19b92` (base `develop@69ab8fc`)  
**Commit:** `docs(ui): close console authority split and wire drift gates`  
**Push:** không

## Đã làm

1. **Authority split.** `docs/README.md`, TL12, `design-system/cmc-edu/{STRUCTURE,PAGE-FRAMES,MASTER}.md`, `packages/ui/llms.txt`, comment chết ở `index.ts` + `console.css`. `primitives.ts:35-41` và `console-navbar.tsx:8` giữ nguyên.
2. **Precedence (hạng mục chính).** `docs/design-system-console.md`: trong `.o_web_client` thì `console.css` cố ý thắng Astryx; ngoài shell (LMS) Astryx/CMC thắng. Trỏ `packages/ui/src/console/console-precedence.test.ts` (file Phase A — **không có trên branch này**). One-door Astryx + SideNav re-export. Bảng cổng CI liệt kê frames / ratchet / a11y-roles / doc-authority (không còn “hai cổng”). FilterBar: đếm, không lock rename.
3. **Hồi sinh cổng chết.** `check:ui-a11y-roles` **xanh** trên HEAD (8/8). Wire vào `ci.yml` cạnh ratchet/frames. Không sửa code app.
4. **Mở scope sang LMS.** Đọc cơ chế trước: ratchet **không** zero-tolerance theo file — fail khi count tăng so với baseline. Admin đang 0. LMS đếm được **61** literal tokenizable / 11 file (số ~77 là mọi `style={{}}`, gồm layout/width/var() được miễn). Ghi 61 vào `scripts/ratchet-baseline.json`. Không cần exemption mới, không đổi thuật toán. Frames mở `apps/lms/src`; `--strict` vẫn xanh (LMS không có EntityHeader/dual-title).
5. **`scripts/check-doc-authority.mjs`** + `scripts/check-doc-authority.test.mjs` + `package.json` + `ci.yml`. Allowlist cứng. Fixture inject `AppFrame` → đỏ; HEAD sạch → xanh.
6. **Hai sửa đọc LMS.** Gỡ `maximum-scale=1.0, user-scalable=no` khỏi `apps/lms/index.html`. Meta bài `2xs`→`sm` tại `student/home.tsx:90`, `exercise.tsx:149,153`, `parent/homework-results.tsx:64`. Không đụng `login.tsx:210,257,272`, `session-evidence.tsx:116,139`, `exercise.tsx:193`.

GitNexus impact trước khi sửa LMS: `StudentHomePage` / `ExercisePage` / `HomeworkResultsPage` / `ExerciseList` / `StarBalanceHero` đều **LOW**. `detect_changes({scope:"all"})`: **LOW**, 0 process.

## Kết quả từng cổng

| Cổng | Kết quả |
|------|---------|
| `pnpm check:ui-a11y-roles` | **0** — 8/8 |
| `pnpm test:ui-a11y-roles` | **0** — 2/2 |
| `pnpm check:ui-frames --strict` | **0** — bulk 8, dual-title 0 |
| `pnpm test:ui-frames` | **0** — 3/3 (có assert LMS root) |
| `pnpm check:ui-ratchet` | **0** — 82 file, 61 LMS grandfathered, increased=[] |
| `pnpm test:ui-ratchet` | **0** — 5/5 |
| `pnpm check:doc-authority` | **0** — 7 file sạch |
| `pnpm test:doc-authority` | **0** — fixture đỏ + HEAD xanh |
| allowlist `rg` AppFrame / `.ck-surface` / `.sh-` / `tpl-wrap` / `.premium-` / `premium.css` | **0 hit** |
| `pnpm --filter @cmc/ui typecheck` | **0** |
| `pnpm typecheck` (turbo) | **đỏ — lỗi sẵn có** (xem dưới) |
| CI wiring | `ci.yml` chạy frames, ratchet, a11y-roles, doc-authority (blocking) |

## `git diff --stat` (cc19b92)

```
 .github/workflows/ci.yml                       |  10 +++
 apps/lms/index.html                            |   2 +-
 apps/lms/src/pages/parent/homework-results.tsx |   2 +-
 apps/lms/src/pages/student/exercise.tsx        |   4 +-
 apps/lms/src/pages/student/home.tsx            |   2 +-
 design-system/cmc-edu/MASTER.md                |  20 ++---
 design-system/cmc-edu/PAGE-FRAMES.md           |  15 ++--
 design-system/cmc-edu/STRUCTURE.md             |   7 +-
 docs/12-design-system-ui.md                    |  37 ++++-----
 docs/README.md                                 |   4 +-
 docs/design-system-console.md                  |  23 +++++-
 package.json                                   |   2 +
 packages/ui/llms.txt                           |   4 +-
 packages/ui/src/console.css                    |   2 +-
 packages/ui/src/index.ts                       |   5 +-
 scripts/check-doc-authority.mjs                | 110 +++++++++++++++++++++++++
 scripts/check-doc-authority.test.mjs           |  65 +++++++++++++++
 scripts/check-ui-frames.mjs                    |  12 ++-
 scripts/check-ui-frames.test.mjs               |   1 +
 scripts/ratchet-baseline.json                  |  16 +++-
 scripts/ui-ratchet.mjs                         |  38 +++++----
 scripts/ui-ratchet.test.mjs                    |  14 ++--
 22 files changed, 306 insertions(+), 89 deletions(-)
```

## Chỗ phải dừng / không ép

1. **`pnpm typecheck` không xanh trên worktree này.** Turbo fail `@cmc/scripts` tại `ensure-shift-catalog.ts:179,181` (`TS7006` implicit any) — file **không** nằm trong diff. Chạy riêng `@cmc/lms` / `@cmc/admin` lộ thêm hàng loạt `type '{}'` trên `apps/api` (Prisma types không resolve). Không vá API/script lạ để ép xanh. `@cmc/ui` (gói TS đụng comment/CSS) typecheck 0.
2. **`console-precedence.test.ts` chưa có trên branch này.** Phase A tạo file đó trên PR khác. Tài liệu trỏ đúng path đặc tả; merge độc lập thì agent mở file sẽ 404 cho đến khi Phase A vào. Không bịa lock thay bằng `console-astryx-remap.test.ts`.
3. **Ratchet LMS không phải zero-tolerance.** 61 literal đếm được → baseline. Không dừng: đúng “thêm một mục baseline”. Không viết exemption từng dòng.
4. **Không đụng** `VIEW-GRAMMAR.md`, `STYLING-BRIDGE.md`, `login.tsx`, `session-evidence.tsx`, `exercise.tsx:193`.
5. Comment `index.ts` bỏ chữ `AppFrame` / `.sh-*` so với câu mẫu spec — nếu giữ, allowlist `rg` và cổng doc-authority đỏ ngay trên file vừa sửa.
