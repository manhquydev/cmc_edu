# Cook — Phase C: Kanban CRM thôi nói dối số (nhịp-1-only)

**Ngày:** 2026-08-13 · **Worktree:** `/home/manhquy/.herdr/worktrees/cmc_edu/fix-crm-kanban-count-truth`  
**Branch:** `fix/crm-kanban-count-truth` · **Base:** `develop@af85b78` (#124 + #125)  
**Commit:** `87f6b307519b9c017b607ec01675e74cc833ca80` (không push)  
**Căn cứ:** `phase-C-crm-kanban-truth.md` + BA Q2 `decisions-ba-260813-0800-outstanding-issues.md`

## Brainstorm contract (tái sử dụng từ phase C)

| Trường | Giá trị |
|---|---|
| Outcome | Badge cột kanban khớp thẻ đang hiện; empty phân biệt chưa-có vs không-trên-trang-này; funnel giữ facility-wide |
| Constraints | `stageCounts` facility-wide (F7); không per-stage query; không đổi contract server; không nới `KanbanColumn` |
| Non-goals | Query per cột, pager per cột, đổi `stageCounts` theo search, nới prop primitive |
| Acceptance | 3 case RTL; `pnpm --filter @cmc/admin test` xanh; diff chỉ 2 file; typecheck xanh |

**Mode:** `/ak-engineer:ak-cook` code (phase file đã chấp nhận) + `/ak-engineer:ak-test`.

## Impact (trước khi sửa)

`impact(CrmPipelinePage, upstream, file_path=apps/admin/src/pages/crm/pipeline.tsx)` → **LOW**, 0 direct callers, 0 processes_affected.

`detect_changes(scope=all)` sau diff → **HIGH**, 2 file / 6 process (`CrmPipelinePage → IsLostOpp|FormatContactPhone|GetOwnerInitials|UseOpportunityActions|Close`). Đây là hub false-positive: sửa biểu thức render badge/empty trong cùng function page, không đổi advance / mark-lost / schedule-test / list contract. Reviewer 9/10 xác nhận không side-effect.

## Đã làm

`apps/admin/src/pages/crm/pipeline.tsx` (vùng 500–525 + comment `byStage`):

- `count={stageCounts[stage.key]}` → `count={stageItems.length}` — badge cột = số thẻ đang hiện.
- Empty tách 2 nhánh:
  - `facilityCount === 0` → `.console-kanban-empty` / `Chưa có`
  - `facilityCount > 0 && stageItems.length === 0` → `.console-kanban-empty` / `Không có trên trang này · N ở giai đoạn`
- FunnelBar `value={stageCounts[stage.key] ?? 0}` không đụng.
- Router và `packages/ui/src/console/console-kanban.tsx` không đụng.

`apps/admin/src/pages/crm/pipeline.test.tsx`:

- Gỡ test cũ khóa `count === stageCounts` (hành vi cũ cố ý đổi, không phải revert vô ý).
- 3 case trên fixture mặc định (`STAGE_COUNTS_MOCK` cố ý lệch items):
  - (a) O1: badge `1` không phải `5`; funnel vẫn `['5','1','2','0','3']`
  - (b) O3: empty `Không có trên trang này · 2 ở giai đoạn`, không `Chưa có`
  - (c) O4: `Chưa có`

## Bằng chứng test đỏ rồi xanh

Tạm để `count={stageCounts[stage.key] ?? 0}` rồi chạy case (a):

```
FAIL  src/pages/crm/pipeline.test.tsx > CrmPipelinePage > renders the O1 column badge from visible cards, not facility-wide stageCounts (funnel stays 5)
AssertionError: expected [ '5', '1', '2', '0', '3' ] to deeply equal [ '1', '2', '0', '0', '0' ]

- Expected
+ Received

  [
+   "5",
    "1",
    "2",
-   "0",
-   "0",
    "0",
+   "3",
  ]

 ❯ src/pages/crm/pipeline.test.tsx:177:23
    expect(colCounts).toEqual(['1', '2', '0', '0', '0']);

 Test Files  1 failed (1)
      Tests  1 failed | 31 skipped (32)
```

Hoàn tác `count={stageItems.length}`:

```
✓ src/pages/crm/pipeline.test.tsx (32 tests | 29 skipped) 441ms
Test Files  1 passed (1)
     Tests  3 passed | 29 skipped (32)
```

Suite đầy đủ (`/ak-engineer:ak-test` → tester):

| Cổng | Kết quả |
|---|---|
| `vitest run src/pages/crm/pipeline.test.tsx` | 32/32 pass |
| `pnpm --filter @cmc/admin test` | 65 files / **625/625** pass |
| `pnpm --filter @cmc/admin typecheck` (không build `^`) | đỏ vì thiếu workspace `dist/` — không phải lỗi Phase C |
| `pnpm typecheck` (turbo, `dependsOn: ["build","^build"]`) | **34/34** successful, 41.5s |

## git diff --stat

```
 apps/admin/src/pages/crm/pipeline.test.tsx | 54 +++++++++++++++++++++++++-----
 apps/admin/src/pages/crm/pipeline.tsx      | 21 +++++++-----
 2 files changed, 58 insertions(+), 17 deletions(-)
```

Chỉ 2 file. Không `router.ts`, không `console-kanban.tsx`.

## Review

`code-reviewer`: **Approved 9/10**. HARD-GATE-NO-SIDE-EFFECTS: không. Cảnh báo thấp (không chặn): copy “không có trên trang này” cũng hiện khi cột trống vì search/`lost` (BA đã chọn copy này); `stageCounts` vẫn stale sau optimistic advance (pre-existing, funnel).

## Docs impact

Không đụng authority surface (`docs/system-architecture.md`, `docs/design-system-console.md`, contract API). Không gọi `docs-manager`.

## Lệch spec

| Điểm | Spec | Thực tế | Lý do |
|---|---|---|---|
| Phạm vi sửa `pipeline.tsx` | “vùng 500-525” | Cũng sửa comment `byStage` (~342–346) | Comment cũ khẳng định badge cột = `stageCounts` — để lại là tài liệu nói dối cùng bất biến vừa phá |
| Empty “không trên trang này” | “không dùng `.console-kanban-empty` «Chưa có» trần” | Vẫn dùng class `.console-kanban-empty`, copy khác | Giữ placeholder nét đứt; invariant (2) cấm combo class+«Chưa có» trần, không cấm class. Reviewer chấp nhận |
| Nghiệm thu PR | `typecheck-and-test` + `ui-e2e` xanh trên PR | Chưa có PR | User cấm push. Local turbo typecheck + admin test đã xanh |
| Browser E2E tay | User rule UI phải soi browser | Không chạy Playwright/browser | Đây là unit/RTL trên jsdom; không đổi layout/route. Không có browser tools gắn app đang chạy |

## Plan sync

- `phase-C-crm-kanban-truth.md`: 3/4 nghiệm thu `[x]`; cổng PR còn `[ ]`
- `plan.md`: hàng C = đã thi hành local; hàng 03 = superseded bởi C

## Commit

```
fix(crm): show kanban column count from visible cards

Badge matches cards on the current page; funnel stays facility-wide.
Empty state distinguishes a truly empty stage from cards off this page.
```

Không push.
