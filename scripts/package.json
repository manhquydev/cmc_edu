{
  "name": "@cmc/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test ./*.test.mjs"
  },
  "dependencies": {
    "@cmc/auth": "workspace:*",
    "@cmc/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "ts-morph": "^24.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
