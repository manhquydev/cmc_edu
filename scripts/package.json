{
  "name": "@cmc/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test ./*.test.mjs && vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@cmc/auth": "workspace:*",
    "@cmc/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "ts-morph": "^24.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
