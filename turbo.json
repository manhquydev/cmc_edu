{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".prisma/**"]
    },
    "typecheck": {
      "dependsOn": ["build", "^build"]
    },
    "test": {
      "dependsOn": ["build", "^build"],
      "env": ["DATABASE_URL", "APP_DATABASE_URL"]
    },

    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
